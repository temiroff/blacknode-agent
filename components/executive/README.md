# Executive

The executive groups task planning, mission execution, installed-skill
selection, consequential-action confirmation, and mission review behind one
public agent component.

Each internal module consumes stable capability contracts. It does not import
robot drivers, transport implementations, or vendor SDKs.
